import React from 'react';
import ReactDOM from 'react-dom';
import FontVoting from './FontVoting';

ReactDOM.render(
  <React.StrictMode>
    <FontVoting />
  </React.StrictMode>,
  document.getElementById('root')
);
